# Session Context & Instructions

**Last Updated:** Tuesday, January 20, 2026
**User:** Pyari-K (pyarisingh@gmail.com)
**Current Goal:** Interview Preparation (SQL Focus)

## Project Status
We are working in the `sql` folder (which is a git repo `pyari-k/SQL-Practise`).
- **Data:** 
  - Customers/Orders: `create_table_*.sql` and `insert_records_*.sql`
  - Music: `create_table_music.sql` and `insert_records_music.sql`
- **Progress:** Completed Practice Questions 01, 02, and 03.

## Environment Setup
- **Working Directory:** Use `interview_prep` as the logical root.
- **Git Identity:** Configured locally as `pyari-k`.
- **Remote:** `git@github.com-pyari:pyari-k/SQL-Practise.git` (Using alias `github.com-pyari`).

## Next Steps
1.  **Resume Practice:** Start with **Practice Question 04**.
2.  **Timer:** The user likes to have a timer for challenges (unless refreshing concepts).
3.  **Style:** 
    - Use CTEs (Common Table Expressions).
    - Be careful with Date filtering (`2023` vs `2003` typo!).
    - Review `LEFT JOIN` vs `NOT IN` logic.

## Commands to Resume
```bash
# 1. Navigate to the folder
cd interview_prep/sql

# 2. Check status
git status
```
